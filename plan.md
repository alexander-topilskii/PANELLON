# План реализации PANELLON

Поэтапный план. Каждый этап даёт **рабочий MVP** — можно запустить и что-то сделать.

---

## Анализ: что не хватает, что разбить

**Не хватает в плане:**

- Raymarching-шаблон — в этапе 3 ✓
- Гибрид, LRU — в этапе 8 ✓
- Пирамида (размер этажа) — не прописана явно
- «Подглядеть снаружи» — README: комната видна через проём до входа. Сложно: рендер Raymarching только в зоне проёма. *
  *Отложить** на этап 9/10, в этапе 3 — только вход
- Этажи 7–20, 21+ — в этапе 5 только 0 и 6. Нужно расширение: этажи 7+ по формуле (автоматически при подъёме)
- smoothMin, bend — в грамматике README, в плане 4d только twist, repeat. Добавить bend в 4d

**Неясно:**

- Лестницы: этаж 6 = 2×2 комнаты. Лестница одна на этаж? Да (по hash в углу)
- Предзагрузка (этап 9): «при приближении к лестнице» — по дистанции. Генерация в main thread (блокирует) или Worker?
  Начать в main thread
- Culling: этаж 100 = 101 комната, 11×11. Рендерить все проёмы или только видимые? Начать без culling (малые этажи),
  оптимизация позже

**Не реалистично за один этап:**

- Этап 8 — кэш + гибрид + LRU + стартовый экран. Разбить на 8a (стартовый экран, позиция), 8b (IndexedDB, LRU), 8c (
  гибрид)
- Этап 6 — этажи 1–5 + пустые ячейки. Разбить на 6a (этажи 1–5), 6b (пустые ячейки)

**Риски:**

- Этап 3: при `isInRoom` — fullscreen Raymarching. «Подглядеть» — позже
- Этаж 0: размер вестибюля — ~6×6 м (узкий, пирамида)
- Физика: коллизия — простая (AABB или «не выходить за bounds»), без physics engine

**Не в плане (сознательно):**

- Чередование типов этажей 1–5 (холл, парковка, колонны) — остаётся один тип
- Culling проёмов на больших этажах — оптимизация позже
- Web Worker для генерации — начать в main thread
- Структура папок проекта — при реализации

---

## Этап 1: Ходьба в пустой комнате

**Цель:** Запустить приложение, ходить от первого лица.

**Задачи:**

- Инициализация проекта (Vite + Three.js)
- Canvas на весь экран
- Камера от первого лица
- WASD + мышь (pointer lock при клике)
- Escape — выход из pointer lock
- Одна пустая комната (box 4×4×2.5 м) — стены, пол, потолок
- Простая физика: Y не ниже пола (0), коллизия — bounds check (без physics engine)

**MVP:** Запускаю → кликаю → хожу по комнате.

---

## Этап 2: Процедурный коридор

**Цель:** Коридор генерируется по seed.

**Задачи:**

- Seed-система: строка → числовой hash (mulberry32 или аналог)
- Один этаж: сетка 3×3 ячеек (упрощённо)
- Коридоры — процедурные меши (стены между ячейками)
- **Коллизия со стенами** (иначе пройдёшь сквозь)
- Детерминизм: один seed = один коридор
- Seed из URL `?seed=...` или дефолт

**MVP:** Меняю seed в URL → вижу другой коридор. Не прохожу сквозь стены.

---

## Этап 3: Первая комната с Raymarching

**Цель:** Одна комната с шейдером, можно зайти.

**Задачи:**

- **Raymarching-шаблон** (универсальный): uniform camera, raymarch loop, слот для `sceneSDF`. Создать один раз.
- Один проём в стене коридора (дверная коробка)
- За проёмом — объём 4×4×2.5 м
- Raymarching: **жёстко зашитая** сфера в шаблоне
- Безшовный вход: камера в том же мире; при позиции «в комнате» — рендер Raymarching вместо мешей (или stencil mask).
  Проще: при входе переключаем рендер на fullscreen quad с Raymarching.
- Коллизия: не выходить за пределы комнаты, не проваливаться

**MVP:** Иду по коридору → вижу проём → заходим → вижу сферу в шейдере.

---

## Этап 4: Грамматика — процедурные комнаты

**Цель:** Содержимое комнаты генерируется по Room_ID.

> Разбито на подэтапы 4a–4d — грамматика объёмная.

### 4a: Шаблон + один примитив

- Room_ID = hash(floor, grid_x, grid_z, seed)
- Генератор: **один** примитив (sphere ИЛИ box ИЛИ cylinder) — выбор по Room_ID % 3
- Параметры (радиус, размер) из seedable PRNG
- Вставка в Raymarching-шаблон (слот `sceneSDF`)
- Fallback при ошибке компиляции — простой gradient

**MVP:** Разные примитивы в разных ячейках.

### 4b: Комбинации (depth 2)

- Дерево: `combine(primitive_a, primitive_b)` — min, max
- Глубина 2: два примитива + одна операция
- Генератор дерева → строка GLSL

**MVP:** Комнаты с двумя объектами (union/intersection).

### 4c: Глубина 1–2 по этажу

- Выбор глубины по floor (6–20 → 1–2)
- Полный набор примитивов: sphere, box, cylinder

**MVP:** На этаже 6 — простые, на 15 — чуть сложнее.

### 4d: Модификаторы (twist, bend, repeat)

- Добавить twist, bend, repeat в грамматику (smoothMin — по желанию)
- Параметры из PRNG

**MVP:** Комнаты с искажёнными формами.

---

## Этап 5: Этажи и лестницы

**Цель:** Можно подниматься и спускаться.

**Задачи:**

- Этаж 0: вестибюль ~6×6 м (узкий, пирамида) + лестница вверх
- Этажи 6+: формула `rooms = floor(1 + 0.01 * floor²)`, side = ceil(√rooms). Этаж 6 → 1 комната → 1×1, этаж 10 → 2 → 2×2, этаж 20 → 5 → 3×3, этаж 100 → 101 → 11×11. **Пирамида:** этаж 0 узкий (6×6 м), с ростом этажа растёт сетка
- Лестницы: одна на этаж, позиция по `hash(floor)` (угол/край сетки)
- Переход между этажами: смена сцены, позиция у лестницы
- UI: номер этажа в углу

**MVP:** Старт на 0 → поднимаюсь на 6, 7, 8… → хожу → спускаюсь.

---

## Этап 6: Этажи 1–5 и пустые ячейки

**Цель:** Полная иерархия этажей, «пустые» комнаты.

> Разбито на 6a и 6b.

### 6a: Этажи 1–5

- Генерация этажей 1, 2, 3, 4, 5 (сейчас только 0 и 6+)
- Одно большое пространство на этаж. **Упрощение:** один тип (длинный коридор)
- Лестница вверх/вниз на каждом

**MVP:** Могу пройти 0 → 1 → 2 → … → 6.

### 6b: Пустые ячейки

- Этажи 6+: часть ячеек — «пустые» (проём + чёрный quad, коллизия блокирует)
- Детекция чёрного экрана: рендер 16×16, яркость < 0.02
- Retry: Room_ID+1… до 50 попыток; при провале — ячейка «пустая»

**MVP:** Есть пустые проёмы и рабочие комнаты.

---

## Этап 7: Полная грамматика по зонам

**Цель:** Сложность растёт с этажом.

> Разбито на подэтапы 7a–7c.

### 7a: Зоны 21–50 (plane, torus, noise3D, rotate_y)

- Примитивы: + plane, torus
- Шум: noise3D (одна функция в шаблоне)
- Анимация: rotate_y(expr, uTime)
- Глубина 2–3

**MVP:** На этаже 25 — комнаты с шумом и вращением.

### 7b: Зоны 51–150 (fbm, fractal_repeat, pulse, slide)

- fbm, displace
- fractal_repeat
- pulse, slide
- Глубина 3–4

**MVP:** На этаже 80 — фрактальные и анимированные комнаты.

### 7c: Зоны 151+ (menger, sierpinski, morph) + цвет

- menger, sierpinski
- morph(expr_a, expr_b, t)
- Цвет в грамматике: градиенты HSV, пульсация hue

**MVP:** На этаже 200 — сложные комнаты с цветом.

---

## Этап 8: Кэш и стартовый экран

**Цель:** Не пересчитывать комнаты, сохранять прогресс.

> Разбито на 8a, 8b, 8c.

### 8a: Стартовый экран и позиция

- Стартовый экран: ввод seed, кнопка «Войти»
- Приоритет: URL > input > localStorage
- Сохранение позиции (этаж, x, z) в localStorage при смене этажа/комнаты
- Восстановление при перезагрузке

**MVP:** Перезагружаю — остаюсь на том же этаже.

### 8b: IndexedDB кэш + LRU

- Store `v{version}:{seed}:{floor}:{x}:{z}` → результат (GLSL или «пусто»)
- При генерации — проверка кэша
- LRU при переполнении (лимит 1 ГБ)

**MVP:** Повторный заход в комнату — без ожидания.

### 8c: Гибрид предгенерация

- При входе на этаж — предгенерация 10–20 ячеек от точки входа (у лестницы)
- Остальные — на лету при приближении

**MVP:** Меньше ожидания при входе на этаж.

---

## Этап 9: Освещение и детали

**Цель:** Атмосфера, лампы, индикатор.

**Задачи:**

- Коридоры: точечные лампы (позиции по hash), тёплый цвет
- Этажи 1–5: мерцание ламп (опционально)
- Комнаты: ключевой свет с направления двери в шейдере
- Лампочка над лестницей — индикатор генерации (мерцание)
- **Подглядеть снаружи** (если не сделано): комната видна через проём до входа — рендер Raymarching в зоне проёма
- Предзагрузка: при дистанции < N до лестницы — запуск генерации следующего этажа (main thread, можно блокировать)

**MVP:** Коридоры освещены, при подъёме — лампочка мигает.

---

## Этап 10: Заархивировать и полировка

**Цель:** Сохранять понравившиеся комнаты, финальный UI.

**Задачи:**

- Кнопка «Заархивировать» в комнате (угол экрана)
- IndexedDB: store архива (код шейдера + метаданные)
- Меню по Escape: настройки (лимит кэша), выход
- WebGL fallback: сообщение при отсутствии поддержки
- Версионирование кэша: константа в коде

**Задачи (опционально):**
- Чередование типов этажей 1–5 (холл, парковка, колонны) — если время

**MVP:** Могу сохранить понравившуюся комнату. Полный цикл: seed → исследование → архив.

---

## Сводка этапов

| Этап | MVP                                             |
|------|-------------------------------------------------|
| 1    | Хожу по комнате                                 |
| 2    | Процедурный коридор по seed                     |
| 3    | Захожу в комнату с Raymarching (шаблон + сфера) |
| 4a   | Один примитив по Room_ID                        |
| 4b   | Два примитива + combine                         |
| 4c   | Глубина по этажу                                |
| 4d   | Модификаторы twist, bend, repeat                |
| 5    | Этажи 0, 6, 7… и лестницы                       |
| 6a   | Этажи 1–5 (один тип)                            |
| 6b   | Пустые ячейки, retry                            |
| 7a   | Зона 21–50: noise, rotate_y                     |
| 7b   | Зона 51–150: fbm, fractal, pulse                |
| 7c   | Зона 151+: menger, morph, цвет                  |
| 8a   | Стартовый экран, сохранение позиции             |
| 8b   | IndexedDB кэш, LRU                              |
| 8c   | Гибрид предгенерация                            |
| 9    | Освещение, лампочка, подглядеть, предзагрузка   |
| 10   | Архив, меню, полировка                          |
